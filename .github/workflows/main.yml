name: CI/CD Dynamic APK Build

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      # KÖKTEN ÇÖZÜM: Android klasörünü silip Flutter CLI ile en güncel, %100 uyumlu haliyle yeniden oluşturuyoruz.
      # Böylece V1 Embedding hatası matematiksel olarak imkansız hale geliyor.
      - name: Regenerate Android Project (Force V2)
        run: |
          rm -rf android
          flutter create --platforms=android .

      - name: Install dependencies
        run: flutter pub get

      # Standart APK Derlemesi
      - name: Build Debug APK
        run: flutter build apk --debug

      # DEV-OPS MÜDAHALESİ: Gradle ile uğraşmak yerine, üretilen APK dosyasının adını dinamik build numarasıyla değiştiriyoruz.
      - name: Rename APK Dynamically
        run: |
          mv build/app/outputs/flutter-apk/app-debug.apk build/app/outputs/flutter-apk/TY-Sozluk-v1.0.${{ github.run_number }}-debug.apk

      # Çıktıyı GitHub Üzerine Yüklüyor
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Dynamic-Debug-APK
          path: build/app/outputs/flutter-apk/TY-Sozluk-v1.0.*-debug.apk
